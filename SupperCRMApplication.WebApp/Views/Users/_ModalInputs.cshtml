@model ModalInputViewModel

<p class="text-sm">
    @Model.Description
</p>
<div class="input-group input-group-outline my-3">
    <label class="form-label">Ad-Soyad</label>
    <input id="name" type="text" class="form-control" @(Model.IsReadonly ? "readonly" : "")>
</div>
<div class="input-group input-group-outline my-3">
    <label class="form-label">E-posta</label>
    <input id="email" type="text" class="form-control" @(Model.IsReadonly ? "readonly" : "")>
</div>
<div class="form-check my-3">
    <input id="islocked" class="form-check-input" type="checkbox" @(Model.IsReadonly ? "disabled" : "")>
    <label class="custom-control-label" for="islocked">Pasif</label>
</div>
<div class="input-group input-group-outline is-filled my-3" >
    <label for="role" class="form-label">Rolü</label>
    <select class="form-control" id="role" @(Model.IsReadonly ? "disabled" : "")>
        <option value="user">Kullanıcı</option>
        <option value="admin">Yönetici</option>
    </select>
</div>
@if(Model.HasId)
{
    <input type="hidden" id="userid" value="" />
}
